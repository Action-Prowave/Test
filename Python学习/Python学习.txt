1.Python的好处：
	软件质量（可读性，一致性，软件质量）（可重用性，可维护性），极简主义的设计理念
	提高开发者的效率（代码量小，不许需要编译以及链接）
	程序的可移植性
	标准库的支持（内置众多预编译应可移植的功能模块-标准库）
	组件集成（集成机制方便与应用程序的其他部分进行通讯）



2.面向对象的脚本语言

3.Python的缺点：
	标准实现方式是：将源代码的预计编译（或者说是转换）为字节码（与平台无关，可移植性）的形式，之后再将字节码解释出来。非二进制代码（运行慢）
